{Make virtual env}
python -m venv virt

{Activate virtual env}
virt\Scripts\activate

{Deactivate virtual env}
deactivate 

{ install command }
pip install pyqt5
pip install pyqt5-tools
pip install mysql-connector-python
pip install pytest
pip install bcrypt

{ command convert dari .ui ke .py , hati hati dengan command ini karena akan menimpa filenya }
pyuic5 -x test.ui -o test.py
pyuic5 -x <nama_file>.ui -o <nama_file_python_baru>.py


pyuic5 -x Home_Klien.ui -o Home_Klien.py